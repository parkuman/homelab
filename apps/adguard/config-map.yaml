apiVersion: v1
kind: ConfigMap
metadata:
  name: adguard-config
  namespace: adguard
data:
  # override the default config to use port 80 instead of the wizard trying to spin up on 3000
  AdGuardHome.yaml: |
    bind_host: 0.0.0.0
    bind_port: 80
