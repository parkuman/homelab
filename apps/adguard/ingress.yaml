apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: adguard
  namespace: adguard
  labels:
    app: adguard
  annotations:
    kubernetes.io/ingress.class: traefik # tells K3S explicitly to use Traefik
    external-dns.alpha.kubernetes.io/hostname: adguard.home.lab
spec:
  rules:
    - host: adguard.home.lab
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: adguard
                port:
                  number: 80
  tls:
    - hosts:
        - adguard.home.lab
      secretName: adguard-tls
