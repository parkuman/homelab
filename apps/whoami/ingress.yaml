apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: whoami
  namespace: whoami
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
    - host: whoami.homelab.local
      http:
        paths:
          - path: /
            pathType: prefix
            backend:
              service:
                name: whoami
                port:
                  number: 80
